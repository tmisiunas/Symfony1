{% extends 'base.html.twig' %}

{% block title %}Tmp{% endblock %}

{% block body %}
    <h1>Tmp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tmp.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tmp.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tmp_index') }}">back to list</a>

    <a href="{{ path('app_tmp_edit', {'id': tmp.id}) }}">edit</a>

    {{ include('tmp/_delete_form.html.twig') }}
{% endblock %}
